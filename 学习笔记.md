# 学习笔记

## 一、AI工具学习

### 1. Vibe Coding 概念理解

Vibe Coding 是由 OpenAI 联合创始人 Andrej Karpathy 提出的编程新范式。简单来说，就是通过自然语言向 AI 描述需求，由 AI 生成并调试代码，开发者从“写代码的人”转变为“提需求的人”和“审代码的人”。

### 2. 提示词工程

- **任务拆解**：将大需求拆成小步骤，分步让 AI 实现（如先生成数据→训练模型→写后端→写前端）。
- **约束说明**：明确技术栈要求（如“用 Flask 写后端”“用 SQLite 存储”）。
- **验收标准**：告诉 AI 什么样算完成（如“返回 JSON 格式”“包含状态字段”）。

### 3. 上下文工程

- 每次新对话时重新提供项目背景信息。
- 让 AI 先阅读现有代码再进行修改。
- 保持对话连贯，不随意切换主题，避免 AI 丢失上下文。

### 4. AI 代码的人工审查

AI生成的代码需要经过理解后再使用。我的做法是：
- 先阅读代码，理解每部分的作用
- 运行测试，发现问题后让 AI 修正
- 确认无误后再提交到 Git 仓库

### 5. 调试与人工接管

遇到 AI 反复修改仍无法解决的问题时：
- 查看错误日志，定位问题来源
- 缩小问题范围，只把相关代码发给 AI 分析
- 必要时手动修改关键部分，再继续让 AI 协助


## 二、拟选用的 Harness 和模型

### 1. 开发工具

| 工具 | 用途 |
| :--- | :--- |
| VS Code | 代码编辑器，集成终端和 Git |
| DeepSeek 网页版（专家模式） | 主要 AI 编程助手 |
| 通义灵码（Tongyi Lingma） | VS Code 辅助插件 |


### 2. 本项目中的实际应用

| 任务 | 使用的工具 | 产出 |
| :--- | :--- | :--- |
| 生成模拟数据 | DeepSeek | generate_data.py |
| 训练分类模型 | DeepSeek | train_model.py + model.pkl |
| 开发后端 API | DeepSeek | app.py |
| 构建前端界面 | DeepSeek | frontend/index.html |
| 自动化测试 | DeepSeek | test_app.py |


## 三、Git 原理与功能

### 1. Git 基本概念

Git 是一个分布式版本控制系统，用于跟踪文件变化，方便代码管理和协作。核心概念如下：

| 概念 | 说明 |
| :--- | :--- |
| 仓库（Repository） | 存放代码和版本记录的地方 |
| 提交（Commit） | 一次保存操作，记录当前所有文件的状态快照 |
| 暂存区（Stage） | 提交前的临时存放区域 |
| 分支（Branch） | 独立的开发线，用于并行开发 |
| 远程仓库（Remote） | 存放在服务器上的仓库，如 GitHub |

### 2. Git 基本命令

| 命令 | 作用 |
| :--- | :--- |
| `git init` | 初始化一个新的 Git 仓库 |
| `git add .` | 将所有更改添加到暂存区 |
| `git commit -m "说明"` | 提交暂存区文件，生成一个版本 |
| `git log` | 查看提交历史 |
| `git remote add origin 地址` | 关联远程仓库 |
| `git push origin main` | 将本地提交推送到远程仓库 |

### 3. 本项目的 Git 使用

- 全程使用 Git 管理代码，每完成一个功能模块即提交一次
- 提交信息具有明确含义（如“完成数据生成和模型训练”）
- 最终推送到 GitHub，生成完整的提交历史供老师查看


## 四、选题调研

### 1. 选题名称

《基于振动特征分析的工业机器人关节健康状态智能诊断系统》

### 2. 选题背景

工业机器人在智能制造中广泛应用，其关节部件长期运行后会出现磨损和抖动加剧的情况，若不及时处理，可能导致设备损坏或生产中断。通过分析传感器数据（振动、温度），可以提前发现设备异常，实现预测性维护。

### 3. 问题定义

给定一组传感器数据（振动值、温度），判断机器人当前健康状态：

| 健康状态 | 特征条件 | 建议 |
| :--- | :--- | :--- |
| 平稳 | 振动 < 2.0，温度 < 50 | 正常使用 |
| 轻微抖动 | 振动 2.0~5.0，温度 50~70 | 建议近期检修 |
| 严重抖动 | 振动 > 5.0，温度 > 70 | 建议立即停机 |

### 4. 数据说明

- 数据来源：由 Python 程序模拟生成，共 1000 条样本
- 特征：振动值（mm/s）、温度（℃）
- 标签：平稳 / 轻微抖动 / 严重抖动（由规则自动打标）

### 5. 系统功能

- 输入振动值和温度，输出健康状态
- 根据状态映射健康度百分比和剩余寿命估算（经验值）
- 保存历史预测记录，支持查询

### 6. 技术可行性

- 随机森林分类器实现简单，分类效果稳定
- Flask 轻量级 Web 框架，适合新手快速开发
- 所有技术栈均为免费开源工具，成本为零


## 五、学习总结

通过本次学习与实践，我对 Vibe Coding 的工作模式有了更深入的理解：

1. **提示质量决定输出质量**：需求描述越具体、任务拆解越清晰，AI 生成的代码可用性越高
2. **人是最终决策者**：AI 生成的代码需要经过人工审查和理解，确保其符合项目要求
3. **版本管理是底线保障**：Git 提供了试错空间，可以随时回滚，降低了学习过程中的风险