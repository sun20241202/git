# 方案设计

## 一、功能需求分析

本系统面向工业机器人关节健康诊断场景，需满足以下功能需求：

| 功能模块 | 需求描述 |
| :--- | :--- |
| 数据生成 | 程序自动生成模拟传感器数据（振动值、温度），用于模型训练 |
| 模型训练 | 基于随机森林算法训练分类模型，识别三种健康状态 |
| 实时预测 | 用户输入振动值和温度，系统返回健康状态（平稳/轻微抖动/严重抖动） |
| 健康评估 | 根据预测状态映射健康度百分比和剩余寿命经验估算值 |
| 历史记录 | 保存每次预测记录，支持查询最近 20 条历史数据 |
| 可视化展示 | 前端仪表盘显示状态指示灯、健康度进度条、历史记录表格 |

## 二、方案论证

### 2.1 技术方案对比

| 方案 | 优点 | 缺点 | 是否采用 |
| :--- | :--- | :--- | :--- |
| 随机森林分类 | 实现简单、可解释性强、适合小数据集 | 对超参数有一定依赖 | ✅ 采用 |
| SVM 分类 | 分类精度高 | 参数调优复杂，训练较慢 | ❌ 不采用 |
| 深度学习（CNN/RNN） | 适合大规模复杂数据 | 数据量不足、训练时间长、部署困难 | ❌ 不采用 |

### 2.2 架构方案对比

| 方案 | 优点 | 缺点 | 是否采用 |
| :--- | :--- | :--- | :--- |
| B/S 架构（浏览器/服务器） | 跨平台、免安装、展示效果好 | 需启动后端服务 | ✅ 采用 |
| C/S 架构（客户端/服务器） | 响应快 | 需安装客户端，跨平台性差 | ❌ 不采用 |

### 2.3 涉及技术方向
1.机器学习与智能决策：scikit-learn 随机森林
2.数据预处理与特征工程：pandas + CSV
3.智能系统集成与Web开发：Flask + SQLite + HTML/JS
4.数据库与数据管理：SQLite


综上，本系统采用 **B/S 架构 + 随机森林分类器** 的技术路线。

## 三、方案规划

### 3.1 技术栈

| 层级 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| 前端 | HTML + CSS + JavaScript（ECharts） | 纯静态页面，调用后端 API 实现数据交互 |
| 后端 | Python 3 + Flask | 轻量级 Web 框架，提供 RESTful API |
| 数据库 | SQLite | 轻量级文件型数据库，无需独立部署 |
| 机器学习 | scikit-learn（RandomForestClassifier） | 实现分类模型训练与预测 |
| 数据生成 | Python + pandas + numpy | 生成模拟传感器数据集 |
| 版本控制 | Git + GitHub | 代码管理与提交记录 |

### 3.2 需配置的外部资源

| 资源类型 | 具体内容 |
| :--- | :--- |
| Python 运行环境 | Python 3.x（已配置） |
| Python 依赖库 | flask, flask-cors, scikit-learn, pandas, joblib, pytest |
| AI 编程工具 | DeepSeek 专家模式（用于代码生成） |
| 代码编辑器 | VS Code（已配置 Git 和 Python 插件） |
| 版本控制 | Git（已安装）、GitHub 账号（已注册） |

### 3.3 数据集来源

- 数据集由 Python 程序自动生成，共 1000 条模拟样本
- 字段包括：时间、关节角度、电机电流、振动值(mm/s)、温度(℃)、健康状态（标签）
- 标签生成规则：
  - 振动 < 2.0 且 温度 < 50 → “平稳”
  - 振动 2.0~5.0 且 温度 50~70 → “轻微抖动”
  - 振动 > 5.0 且 温度 > 70 → “严重抖动”
- 训练集占 80%，测试集占 20%

### 3.4 前端 UI 方案

- 深蓝色科技感仪表盘风格
- 核心交互区：两个输入框（振动值、温度）+ 预测按钮
- 结果展示区：状态文字 + 绿/黄/红指示灯 + 健康度进度条 + 剩余寿命描述
- 历史记录区：表格展示最近 20 条预测记录（时间、振动值、温度、状态）

### 3.5 后端算法方案

| 模块 | 算法/方法 | 说明 |
| :--- | :--- | :--- |
| 健康状态分类 | 随机森林分类器 | 输入振动值、温度，输出平稳/轻微抖动/严重抖动 |
| 健康度映射 | 经验规则 | 平稳→85%，轻微抖动→60%，严重抖动→30% |
| 剩余寿命估算 | 经验规则 | 平稳→800+小时，轻微抖动→300小时，严重抖动→立即检修 |
| 数据存储 | SQLite 数据库 | 预测记录持久化，支持历史查询 |

## 四、技术路线
需求分析 → 数据生成 → 模型训练 → 后端开发 → 前端开发 → 集成测试 → 部署演示、

详细分解如下：

1. **需求分析**：明确系统功能与交互流程
2. **数据生成**：编写 `generate_data.py`，生成 1000 条模拟数据
3. **模型训练**：编写 `train_model.py`，训练随机森林分类器，保存为 `model.pkl`
4. **后端开发**：编写 `app.py`，提供 `/predict` 和 `/history` 接口，连接 SQLite 数据库
5. **前端开发**：编写 `frontend/index.html`，实现仪表盘界面与 API 调用
6. **集成测试**：编写 `test_app.py`，使用 unittest 框架自动化测试接口
7. **部署演示**：本地运行系统，录制 3 分钟演示视频，制作答辩 PPT

## 五、计划安排

| 阶段 | 任务内容 |
| :--- | :--- |
| 第一阶段| 工具配置、Git 环境搭建、选题调研
| 第二阶段  数据生成脚本编写与模型训练
| 第三阶段| 后端 API 开发与数据库设计
| 第四阶段| 前端界面开发与前后端联调
| 第五阶段 |集成测试、健康寿命功能添加
| 第六阶段| 报告撰写、PPT 制作、视频录制

## 六、参考文献

[1] 《制造智能技术基础》：清华大学出版社，2022.

[2] 《人工智能基础》：机械工业出版社，2025.

[3] Flask 官方文档：https://flask.palletsprojects.com/

[4] GitHub 文档：https://docs.github.com/